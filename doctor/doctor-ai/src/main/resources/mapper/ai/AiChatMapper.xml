<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doctor.ai.mapper.AiChatMapper">
    
    <resultMap type="AiChat" id="AiChatResult">
        <id     property="chatId"           column="chat_id"         />
        <result property="userId"           column="user_id"         />
        <result property="assistantType"    column="assistant_type"  />
        <result property="title"            column="title"           />
        <result property="lastMessageTime"  column="last_message_time"/>
        <result property="delFlag"          column="del_flag"        />
        <result property="createBy"         column="create_by"       />
        <result property="createTime"       column="create_time"     />
        <result property="updateBy"         column="update_by"       />
        <result property="updateTime"       column="update_time"     />
    </resultMap>

    <sql id="selectAiChatVo">
        select chat_id, user_id, assistant_type, title, last_message_time, del_flag, create_by, create_time, update_by, update_time 
        from ai_chat
    </sql>

    <select id="selectAiChatListByUserId" parameterType="Long" resultMap="AiChatResult">
        <include refid="selectAiChatVo"/>
        where user_id = #{userId} and del_flag = '0'
        order by last_message_time desc
    </select>
    
    <select id="selectAiChatById" parameterType="Long" resultMap="AiChatResult">
        <include refid="selectAiChatVo"/>
        where chat_id = #{chatId} and del_flag = '0'
    </select>
        
    <insert id="insertAiChat" parameterType="AiChat" useGeneratedKeys="true" keyProperty="chatId">
        insert into ai_chat
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="assistantType != null">assistant_type,</if>
            <if test="title != null and title != ''">title,</if>
            <if test="lastMessageTime != null">last_message_time,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null and updateBy != ''">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            del_flag
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="assistantType != null">#{assistantType},</if>
            <if test="title != null and title != ''">#{title},</if>
            <if test="lastMessageTime != null">#{lastMessageTime},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null and updateBy != ''">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            '0'
        </trim>
    </insert>

    <update id="updateAiChat" parameterType="AiChat">
        update ai_chat
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="assistantType != null">assistant_type = #{assistantType},</if>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="lastMessageTime != null">last_message_time = #{lastMessageTime},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where chat_id = #{chatId}
    </update>

    <update id="deleteAiChatById" parameterType="Long">
        delete from ai_chat where chat_id = #{chatId}
    </update>

    <update id="deleteAiChatByIds" parameterType="String">
        delete from ai_chat where chat_id in 
        <foreach item="chatId" collection="array" open="(" separator="," close=")">
            #{chatId}
        </foreach>
    </update>
    
</mapper> 
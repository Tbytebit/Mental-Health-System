<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doctor.survey.mapper.SurveyQuestionnaireRespondentsMapper">

    <resultMap id="BaseResultMap" type="com.doctor.survey.domain.SurveyQuestionnaireRespondents">
            <id property="questionnaire_id" column="questionnaire_id" jdbcType="BIGINT"/>
            <id property="respondents_id" column="respondents_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        questionnaire_id,respondents_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_questionnaire_respondents
        where  questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND respondents_id = #{respondents_id,jdbcType=BIGINT}
    </select>
    <select id="selectByQuestionnaireId" parameterType="String" resultType="String">
        select respondents_id
        <include refid="Base_Column_List"/>
        from survey_questionnaire_respondents
        where questionnaire_id = #{questionnnaire_id,jdbcType=BIGINT}
    </select>

    <select id="selectByRespondentsId" parameterType="String" resultMap="BaseResultMap" >
        select
        <include refid="Base_Column_List" />
        from survey_questionnaire_respondents
        where respondents_id = #{respondents_id,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from survey_questionnaire_respondents
        where  questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND respondents_id = #{respondents_id,jdbcType=BIGINT}
    </delete>
    <delete id="deleteByRespondentsIds" parameterType="String">
        delete from survey_questionnaire_respondents where respondents_id in
        <foreach item="respondentsId" collection="array" open="(" separator="," close=")">
            #{respondentsId}
        </foreach>
    </delete>
    <insert id="insert">
        insert into survey_questionnaire_respondents
        ( questionnaire_id,respondents_id)
        values (#{questionnaire_id,jdbcType=BIGINT},#{respondents_id,jdbcType=BIGINT})
    </insert>
    <insert id="insertSelective">
        insert into survey_questionnaire_respondents
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="questionnaire_id != null">questionnaire_id,</if>
                <if test="respondents_id != null">respondents_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="questionnaire_id != null">#{questionnaire_id,jdbcType=BIGINT},</if>
                <if test="respondents_id != null">#{respondents_id,jdbcType=BIGINT},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.doctor.survey.domain.SurveyQuestionnaireRespondents">
        update survey_questionnaire_respondents
        <set>
        </set>
        where   questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND respondents_id = #{respondents_id,jdbcType=BIGINT}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.doctor.survey.domain.SurveyQuestionnaireRespondents">
        update survey_questionnaire_respondents
        set 
        where   questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND respondents_id = #{respondents_id,jdbcType=BIGINT}
    </update>
</mapper>

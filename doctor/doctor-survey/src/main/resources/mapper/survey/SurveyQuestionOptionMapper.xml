<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doctor.survey.mapper.SurveyQuestionOptionMapper">

    <resultMap id="BaseResultMap" type="com.doctor.survey.domain.SurveyQuestionOption">
            <id property="question_id" column="question_id" jdbcType="BIGINT"/>
            <id property="option_id" column="option_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        question_id,option_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_question_option
        where  question_id = #{question_id,jdbcType=BIGINT} AND option_id = #{option_id,jdbcType=BIGINT} 
    </select>

    <select id="selectListById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_question_option
        where  question_id = #{question_id,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from survey_question_option
        where  question_id = #{question_id,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByOptionId" parameterType="java.lang.Long">
        delete from survey_question_option
        where option_id = #{option_id,jdbcType=BIGINT}
    </delete>
    <insert id="insert">
        insert into survey_question_option
        ( question_id,option_id)
        values (#{question_id,jdbcType=BIGINT},#{option_id,jdbcType=BIGINT})
    </insert>
    <insert id="insertSelective">
        insert into survey_question_option
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="question_id != null">question_id,</if>
                <if test="option_id != null">option_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="question_id != null">#{question_id,jdbcType=BIGINT},</if>
                <if test="option_id != null">#{option_id,jdbcType=BIGINT},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.doctor.survey.domain.SurveyQuestionOption">
        update survey_question_option
        <set>
        </set>
        where   question_id = #{question_id,jdbcType=BIGINT} AND option_id = #{option_id,jdbcType=BIGINT} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.doctor.survey.domain.SurveyQuestionOption">
        update survey_question_option
        set 
        where   question_id = #{question_id,jdbcType=BIGINT} AND option_id = #{option_id,jdbcType=BIGINT} 
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doctor.survey.mapper.SurveyQuestionnaireQuestionMapper">

    <resultMap id="BaseResultMap" type="com.doctor.survey.domain.SurveyQuestionnaireQuestion">
            <id property="questionnaire_id" column="questionnaire_id" jdbcType="BIGINT"/>
            <id property="question_id" column="question_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        questionnaire_id,question_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_questionnaire_question
        where  questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND question_id = #{question_id,jdbcType=BIGINT} 
    </select>

    <select id="selectListByQuestionnaireId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_questionnaire_question
        where  questionnaire_id = #{questionnaire_id,jdbcType=BIGINT}
    </select>
    <select id="getQuestionnaireId" resultType="java.lang.String">
        select questionnaire_id
        from survey_questionnaire_question
        where question_id = #{question_id,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from survey_questionnaire_question
        where  question_id = #{question_id,jdbcType=BIGINT}
    </delete>
    <insert id="insert">
        insert into survey_questionnaire_question
        ( questionnaire_id,question_id)
        values (#{questionnaire_id,jdbcType=BIGINT},#{question_id,jdbcType=BIGINT})
    </insert>
    <insert id="insertSelective">
        insert into survey_questionnaire_question
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="questionnaire_id != null">questionnaire_id,</if>
                <if test="question_id != null">question_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="questionnaire_id != null">#{questionnaire_id,jdbcType=BIGINT},</if>
                <if test="question_id != null">#{question_id,jdbcType=BIGINT},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.doctor.survey.domain.SurveyQuestionnaireQuestion">
        update survey_questionnaire_question
        <set>
        </set>
        where   questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND question_id = #{question_id,jdbcType=BIGINT} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.doctor.survey.domain.SurveyQuestionnaireQuestion">
        update survey_questionnaire_question
        set 
        where   questionnaire_id = #{questionnaire_id,jdbcType=BIGINT} AND question_id = #{question_id,jdbcType=BIGINT} 
    </update>

</mapper>

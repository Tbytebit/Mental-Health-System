<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doctor.survey.mapper.SurveyRespondentsAnswerMapper">

    <resultMap id="BaseResultMap" type="com.doctor.survey.domain.SurveyRespondentsAnswer">
            <id property="respondents_id" column="respondents_id" jdbcType="BIGINT"/>
            <id property="answer_id" column="answer_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        respondents_id,answer_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from survey_respondents_answer
        where  respondents_id = #{respondents_id,jdbcType=BIGINT} AND answer_id = #{answer_id,jdbcType=BIGINT} 
    </select>

    <select id="selectByRespondentsIds" parameterType="String" resultType="java.lang.String">
        select answer_id
        <include refid="Base_Column_List"/>
        from survey_respondents_answer
        where respondents_id in
        <foreach item="respondentsId" collection="array" open="(" separator="," close=")">
            #{respondentsId}
        </foreach>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from survey_respondents_answer
        where  respondents_id = #{respondents_id,jdbcType=BIGINT} AND answer_id = #{answer_id,jdbcType=BIGINT} 
    </delete>

    <delete id="deleteByRespondentsIds" parameterType="String">
        delete from survey_respondents_answer
        where respondents_id in
        <foreach item="respondentsId" collection="array" open="(" separator="," close=")">
            #{respondentsId}
        </foreach>
    </delete>

    <insert id="insert">
        insert into survey_respondents_answer
        ( respondents_id,answer_id)
        values (#{respondents_id,jdbcType=BIGINT},#{answer_id,jdbcType=BIGINT})
    </insert>
    <insert id="insertSelective">
        insert into survey_respondents_answer
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="respondents_id != null">respondents_id,</if>
                <if test="answer_id != null">answer_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="respondents_id != null">#{respondents_id,jdbcType=BIGINT},</if>
                <if test="answer_id != null">#{answer_id,jdbcType=BIGINT},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.doctor.survey.domain.SurveyRespondentsAnswer">
        update survey_respondents_answer
        <set>
        </set>
        where   respondents_id = #{respondents_id,jdbcType=BIGINT} AND answer_id = #{answer_id,jdbcType=BIGINT} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.doctor.survey.domain.SurveyRespondentsAnswer">
        update survey_respondents_answer
        set 
        where   respondents_id = #{respondents_id,jdbcType=BIGINT} AND answer_id = #{answer_id,jdbcType=BIGINT} 
    </update>
</mapper>
